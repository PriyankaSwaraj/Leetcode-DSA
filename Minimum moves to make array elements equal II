class Solution {
    public int minMoves2(int[] nums) {
    if(nums.length<2)
    {
        return 0;
    }
   Arrays.sort(nums);
   int n=nums.length,count=0;
   for(int i=0;i<n;i++)
   {
    count += Math.abs(nums[n/2]-nums[i]);
   }
    return count;
    }
}
