class Solution {
    public int maxSubArray(int[] nums) {
    int sum =nums[0],max=nums[0],n=nums.length;
    for(int i=1;i<n;i++)
    {
        sum = Math.max(sum+nums[i],nums[i]);
        max = Math.max(max,sum);
    }
    return max;   
    }
}
