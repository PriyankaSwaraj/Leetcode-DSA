class Solution {
    public int titleToNumber(String columnTitle) {
      int count=0,n=columnTitle.length();
      for(int i=0,j=n-1;i<n;i++,j--)
      {
        int a =(int)Math.pow(26,j);
        count += a*(columnTitle.charAt(i)-64);  
      }
      return count; 
    }
}
